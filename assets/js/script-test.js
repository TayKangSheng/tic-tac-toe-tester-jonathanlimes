function ticTacToe () {
  var $grid = $('.container div')
  var player = 'X'

  $grid.forEach(function (box) {
    $grid
  })

  function clickBox () {}
  function restart () {}
  function isGameOver () {}
  function whoWon () {}
  function playTurn () {}

  return {
    clickBox: clickBox,
    restart: restart,
    isGameOver: isGameOver,
    whoWon: whoWon,
    playTurn: playTurn
  }
}

window.addEventListener('DOMContentLoaded', function () {
  ticTacToe()
})
